# Colab-focused requirements for running the IntentWatch backend demo.
# IMPORTANT: Do NOT pin/install torch/torchvision here — Colab already provides a CUDA build.

fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.7
python-dotenv>=1.0.0
opencv-python==4.9.0.80
numpy<2
ultralytics==8.1.0
# Optional (only needed if you want Supabase uploads in the demo)
# supabase>=2.3.0

pyngrok>=7.2.0
requests>=2.31.0
